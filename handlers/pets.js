'use strict';

/**
 * Operations on /pets
 */
module.exports = {
    
    /**
     * 
     * parameters: limit
     * produces: 
     */
    get: function listPets(req, res) {
        res.sendStatus(501);
    }, 
    
    /**
     * 
     * parameters: 
     * produces: 
     */
    post: function createPets(req, res) {
        res.sendStatus(501);
    }
    
};
