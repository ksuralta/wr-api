'use strict';

/**
 * Operations on /pets/{petId}
 */
module.exports = {
    
    /**
     * 
     * parameters: petId
     * produces: 
     */
    get: function showPetById(req, res) {
        res.sendStatus(501);
    }
    
};
